<%
raise "No RACK_ENV or RAILS_ENV found" unless ENV["RAILS_ENV"] || ENV["RACK_ENV"]
raise "No SECRET_KEY_BASE found" unless ENV['SECRET_KEY_BASE']
%>---
<%= ENV["RAILS_ENV"] || ENV["RACK_ENV"] %>:
  secret_key_base: "<%= ENV['SECRET_KEY_BASE'] %>"
